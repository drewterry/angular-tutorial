version: '3'
services:
  web:
    build: .
    command: /bin/bash
    volumes:
      - ./angular-tour-of-heroes:/angular-tour-of-heroes
      - /angular-tour-of-heroes/node_modules
    ports:
      - "4200:4200"
